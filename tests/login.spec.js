import { test, expect } from '@playwright/test';

// test.beforeEach(async()=>{
//     await page.goto('https://demoqa.com/login',{timeout:90000});
// })
test('Login via Authenticate Setup', async ({ page }) => {
   page.goto('/')

});